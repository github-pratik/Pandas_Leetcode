import pandas as pd

def find_patients(patients: pd.DataFrame) -> pd.DataFrame:
    patients_result = patients[patients['conditions'].str.contains(r'\bDIAB1')]

    result  = patients_result[['patient_id','patient_name','conditions']]

    return result
    
